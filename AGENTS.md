# AGENTS.md instructions for D:\Code\cflow

## 工程原则

1. 先判断问题类型，再决定修法。不要扩大边界，但有必要重构的一定要重构。
2. 要按根因收口，不按现象修补。先确定单一来源和唯一真实边界，再一次性迁移所有调用点。确保原功能完整迁移后，禁止保留兼容层、旧类型、旧 helper、旧恢复逻辑、旧导出。不允许停留在半迁移状态。不要做到“可用”，要做到“旧架构已经不存在”。
3. 如果项目是本地桌面应用，不应过度纠结安全问题。
4. 如果需要下载或安装文件，尽量不要使用 C 盘空间。

## CFlow 默认路由

在本仓库内，只要用户请求属于 CFlow 内容生产、写作采访、选题、角度、研究、案例、成稿、改稿、质检、包装、营销、短内容、视觉、SEO、传播、复盘、吸收进化、账号诊断、作者声音或 skill 治理维护，即使用户没有显式点名 skill，也必须先按 `$cflow` 主入口判断。

执行方式：

1. 先读取并遵守 `skills/cflow/SKILL.md`。
2. 按 `$cflow` 主入口选择特殊直达 skill 或固定线性流程。
3. 进入具体节点时，再读取对应 `skills/<skill-name>/SKILL.md`。
4. 如果用户已经显式点名某个 CFlow skill，并且任务和该 skill 基本匹配，尊重点名入口。
5. 只有用户明确说“不要走 CFlow”“普通回答”“不要用 skill”时，才跳过 CFlow 默认路由。

不要把未点名 skill 当成普通聊天直接处理。CFlow 相关任务的默认入口是 `$cflow`，不是模型自己的临场判断。

## 非 CFlow 请求

普通代码修改、文件操作、命令执行、git 操作、环境排查或仓库工程任务，按 Codex 工程流程处理，不强行套入 CFlow 内容生产流程。
